# Configuration npm pour le monorepo

# Utiliser la version locale du package lors de la résolution des dépendances
legacy-peer-deps=false

# Activer les strict-peer-dependencies par défaut
strict-peer-deps=false
