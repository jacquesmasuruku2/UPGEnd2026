{
  "name": "upg-cloudflare-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "d1:apply:local": "wrangler d1 migrations apply upg-d1-preview --local",
    "d1:apply:remote": "wrangler d1 migrations apply upg-d1-preview --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.99.0"
  }
}
